import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ai.rinklinkai.mobile',
  appName: 'RinkLink.ai',
  webDir: '../../dist/apps/mobile/browser',
};

export default config;
