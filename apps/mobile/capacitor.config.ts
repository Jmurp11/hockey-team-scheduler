import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hockeyteamscheduler.mobile',
  appName: 'Hockey Team Scheduler',
  webDir: '../../dist/apps/mobile/browser',
};

export default config;
